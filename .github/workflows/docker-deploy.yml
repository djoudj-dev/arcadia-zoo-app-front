name: Docker Deploy

on:
  push:
    branches:
      - main
      - develop

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: ${{ secrets.DOCKER_USERNAME }}/arcadia-zoo-app-front:latest

      - name: Add server to known hosts
        run: |
          echo "Creating .ssh directory and setting permissions..."
          mkdir -p ~/.ssh
          chmod 700 ~/.ssh
          echo "Running ssh-keyscan to add the server to known hosts..."
          ssh-keyscan -H 212.227.26.187 >> ~/.ssh/known_hosts
          chmod 600 ~/.ssh/known_hosts
          echo "ssh-keyscan completed successfully."

      - name: Set up SSH agent and add private key
        run: |
          eval "$(ssh-agent -s)"
          echo "${{ secrets.SSH_PRIVATE_KEY }}" | tr -d '\r' | ssh-add -
          ssh-add -l # Vérifier les clés chargées dans l'agent SSH
        shell: bash

      - name: Deploy to server
        run: |
          ssh -o StrictHostKeyChecking=no root@212.227.26.187 "docker pull ${{ secrets.DOCKER_USERNAME }}/arcadia-zoo-app-front:latest && docker-compose up -d"
        shell: bash
